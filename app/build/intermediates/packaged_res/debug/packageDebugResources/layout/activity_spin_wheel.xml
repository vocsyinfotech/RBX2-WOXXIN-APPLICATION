<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#F5F5F5">

    <LinearLayout android:layout_width="match_parent" android:layout_height="56dp"
        android:background="@color/theme_color" android:orientation="horizontal"
        android:gravity="center_vertical" android:paddingHorizontal="8dp">
        <ImageButton android:id="@+id/btnBack" android:layout_width="48dp" android:layout_height="48dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_back_arrow" android:tint="#FFFFFF" android:contentDescription="Back"/>
        <TextView android:layout_width="0dp" android:layout_height="wrap_content" android:layout_weight="1"
            android:text="Spin Wheel" android:textColor="#FFFFFF" android:textSize="18sp"
            android:textStyle="bold" android:layout_marginStart="4dp"/>
    </LinearLayout>

    <LinearLayout android:layout_width="match_parent" android:layout_height="0dp"
        android:layout_weight="1" android:orientation="vertical" android:gravity="center"
        android:padding="20dp">

        <!-- Spin pointer -->
        <TextView android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="🔽" android:textSize="36sp" android:layout_gravity="center"/>

        <!-- Spin wheel image (rotated by animator) -->
        <ImageView
            android:id="@+id/ivSpinWheel"
            android:layout_width="280dp"
            android:layout_height="280dp"
            android:src="@drawable/spin_wheel_bg"
            android:contentDescription="Spin Wheel"
            android:layout_gravity="center" />

        <TextView android:id="@+id/tvSpinResult"
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="Press SPIN to play!" android:textSize="20sp" android:textStyle="bold"
            android:textColor="@color/theme_color" android:layout_marginTop="24dp"
            android:gravity="center" android:layout_gravity="center"/>

        <Button android:id="@+id/btnSpin"
            android:layout_width="180dp" android:layout_height="56dp"
            android:layout_marginTop="20dp" android:text="🎡  SPIN!" android:textSize="18sp"
            android:textStyle="bold" android:backgroundTint="@color/theme_color"/>
    </LinearLayout>
</LinearLayout>
