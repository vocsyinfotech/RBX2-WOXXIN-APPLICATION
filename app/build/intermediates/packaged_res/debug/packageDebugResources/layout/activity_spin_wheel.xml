<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:fitsSystemWindows="true"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg_primary">

    <!-- Toolbar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:background="@color/bg_surface"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="8dp">

        <ImageButton
            android:id="@+id/btnBack"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_back_arrow"
            android:tint="@color/gold"
            android:contentDescription="Back" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Spin &amp; Wheel"
            android:textColor="@color/gold"
            android:textSize="20sp"
            android:textStyle="bold"
            android:gravity="center"
            android:letterSpacing="0.02" />

        <View android:layout_width="48dp" android:layout_height="48dp" />
    </LinearLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/bg_divider" />

    <!-- Scrollable content — prevents cutting on any screen size -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:fillViewport="true"
        android:clipChildren="false">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center_horizontal"
            android:paddingTop="16dp"
            android:paddingBottom="24dp"
            android:paddingHorizontal="24dp"
            android:clipChildren="false">

            <!-- Fixed gold pointer -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="▼"
                android:textSize="26sp"
                android:textColor="@color/gold"
                android:layout_marginBottom="0dp" />

            <!-- Wheel — fixed 300×300 dp, no clipping issues -->
            <rbx.rbxcalculator.calctool.app.robox.spin.WheelView
                android:id="@+id/wheelView"
                android:layout_width="300dp"
                android:layout_height="300dp" />

            <!-- Gold stand base — placed directly below wheel, no negative margins -->
            <View
                android:layout_width="200dp"
                android:layout_height="48dp"
                android:layout_marginTop="-8dp"
                android:background="@drawable/spin_wheel_stand" />

            <!-- Result card -->
            <androidx.cardview.widget.CardView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="20dp"
                android:layout_marginBottom="16dp"
                app:cardCornerRadius="14dp"
                app:cardElevation="0dp"
                app:cardBackgroundColor="@color/bg_surface">

                <TextView
                    android:id="@+id/tvSpinResult"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Tap Spin Now to play!"
                    android:textSize="16sp"
                    android:textStyle="bold"
                    android:textColor="@color/text_primary"
                    android:padding="16dp"
                    android:gravity="center" />
            </androidx.cardview.widget.CardView>

            <!--
                FIX: Use AppCompatButton (style Widget.AppCompat.Button) so Material
                doesn't override android:background with colorPrimary backgroundTint.
                Text is white on dark background — clearly readable.
            -->
            <androidx.appcompat.widget.AppCompatButton
                android:id="@+id/btnSpin"
                android:layout_width="match_parent"
                android:layout_height="60dp"
                android:text="Spin Now"
                android:textSize="18sp"
                android:textStyle="bold"
                android:textColor="@color/gold"
                android:background="@drawable/btn_spin_now"
                android:stateListAnimator="@null" />

        </LinearLayout>
    </ScrollView>
</LinearLayout>
